.feedback-container {
  padding: 18px;
  max-width: 1200px;
  margin: 0 auto;
}

.page-header {
  margin-bottom: 24px;
  
  h1 {
    font-size: 28px;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 8px 0;
  }
  
  .subtitle {
    color: var(--text-secondary);
    margin: 0;
  }
}

// Accuracy Cards
.accuracy-cards {
  display: grid;
  grid-template-columns: 2fr repeat(3, 1fr);
  gap: 16px;
  margin-bottom: 24px;
  
  @media (max-width: 900px) {
    grid-template-columns: repeat(2, 1fr);
  }
  
  @media (max-width: 600px) {
    grid-template-columns: 1fr;
  }
}

.accuracy-card {
  background: rgba(255, 255, 255, 0.88);
  border-radius: 14px;
  padding: 20px;
  display: flex;
  align-items: center;
  gap: 16px;
  border: 1px solid rgba(15, 118, 110, 0.12);
  box-shadow: var(--shadow-glass);
  
  &.main {
    flex-direction: column;
    align-items: flex-start;
    
    .card-content {
      align-items: flex-start;
    }
  }
  
  .card-icon {
    font-size: 32px;
  }
  
  .card-content {
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  
  .card-value {
    font-size: 28px;
    font-weight: 700;
    color: var(--text-primary);
    
    &.value-success { color: #28a745; }
    &.value-warning { color: #ffc107; }
    &.value-danger { color: #dc3545; }
  }
  
  .card-label {
    font-size: 14px;
    color: var(--text-secondary);
  }
  
  .card-breakdown {
    display: flex;
    gap: 12px;
    margin-top: 8px;
    
    .breakdown-item {
      padding: 4px 8px;
      border-radius: 4px;
      font-size: 13px;
      font-weight: 500;
      
      &.success { background: #d4edda; color: #155724; }
      &.warning { background: #fff3cd; color: #856404; }
      &.danger { background: #f8d7da; color: #721c24; }
    }
  }
}

// Calibration Section
.calibration-section {
  margin-bottom: 24px;
}

.calibration-card {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 14px;
  padding: 20px;
  border: 1px solid rgba(15, 118, 110, 0.12);
  box-shadow: var(--shadow-glass);
  
  &.needs-calibration {
    border-left: 4px solid #ffc107;
  }
  
  .calibration-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 16px;
    
    h3 {
      margin: 0;
    }
  }
  
  .calibration-badge {
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 12px;
    font-weight: 500;
    background: #d4edda;
    color: #155724;
    
    &.active {
      background: #fff3cd;
      color: #856404;
    }
  }
  
  .calibration-stats {
    display: flex;
    gap: 32px;
    
    @media (max-width: 600px) {
      flex-direction: column;
      gap: 16px;
    }
    
    .stat {
      display: flex;
      flex-direction: column;
      
      .stat-value {
        font-size: 24px;
        font-weight: 600;
      }
      
      .stat-label {
        font-size: 13px;
        color: var(--text-secondary);
      }
      
      &.trend {
        &.trend-improving .stat-value { color: #28a745; }
        &.trend-declining .stat-value { color: #dc3545; }
        &.trend-stable .stat-value { color: #6c757d; }
      }
    }
  }
  
  .last-calibration {
    margin: 12px 0 0 0;
    font-size: 13px;
    color: var(--text-secondary);
  }
}

// Action Bar
.action-bar {
  margin-bottom: 24px;
  display: flex;
  gap: 12px;
}

// Success Banner
.success-banner {
  background: #d4edda;
  color: #155724;
  padding: 12px 16px;
  border-radius: 8px;
  margin-bottom: 16px;
}

// Select dropdown
select {
  width: 100%;
  padding: 10px 12px;
  border: 1px solid #ddd;
  border-radius: 6px;
  font-size: 14px;
  background: white;
  
  &:focus {
    outline: none;
    border-color: var(--brand);
    box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.12);
  }
}

// Buttons
.btn {
  padding: 10px 20px;
  border: none;
  border-radius: 10px;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.2s;
  
  &.btn-primary {
    background: var(--brand);
    color: white;
    
    &:hover {
      background: var(--brand-strong);
    }
  }
  
  &.btn-secondary {
    background: #e9ecef;
    color: #495057;
    
    &:hover {
      background: #dee2e6;
    }
  }
  
  &.btn-sm {
    padding: 6px 12px;
    font-size: 13px;
  }
}

.btn-close {
  background: none;
  border: none;
  font-size: 24px;
  cursor: pointer;
  color: #666;
  
  &:hover {
    color: #333;
  }
}

// Feedback Form
.feedback-form-container {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 14px;
  padding: 24px;
  border: 1px solid rgba(15, 118, 110, 0.12);
  box-shadow: var(--shadow-soft);
  margin-bottom: 24px;
  
  .form-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
    
    h2 {
      margin: 0;
    }
  }
}

.form-row {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
  
  @media (max-width: 600px) {
    grid-template-columns: 1fr;
  }
}

.form-group {
  margin-bottom: 16px;
  
  label {
    display: block;
    font-weight: 500;
    margin-bottom: 8px;
    color: #333;
  }
  
  input[type="text"],
  input[type="number"],
  textarea {
    width: 100%;
    padding: 10px 12px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-size: 14px;
    
    &:focus {
      outline: none;
      border-color: var(--brand);
      box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.12);
    }
  }
  
  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    
    input {
      width: 18px;
      height: 18px;
    }
  }
}

.agreement-options {
  display: flex;
  gap: 12px;
  
  @media (max-width: 600px) {
    flex-direction: column;
  }
  
  .agreement-option {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 16px;
    border: 2px solid rgba(15, 35, 42, 0.12);
    border-radius: 12px;
    cursor: pointer;
    transition: all 0.2s;
    
    input {
      display: none;
    }
    
    &.selected {
      border-color: var(--brand);
      background: #eaf8f6;
    }
    
    &:hover {
      border-color: var(--brand);
    }
    
    .option-icon {
      font-size: 32px;
      margin-bottom: 8px;
    }
    
    .option-text {
      font-weight: 500;
      text-align: center;
    }
  }
}

.rating-input {
  display: flex;
  align-items: center;
  gap: 16px;
  
  input[type="range"] {
    flex: 1;
    height: 8px;
    appearance: none;
    -webkit-appearance: none;
    background: #e9ecef;
    border-radius: 4px;
    
    &::-webkit-slider-thumb {
      -webkit-appearance: none;
      width: 20px;
      height: 20px;
      background: var(--brand);
      border-radius: 50%;
      cursor: pointer;
    }
  }
  
  .rating-display {
    font-size: 20px;
    color: #ffc107;
    min-width: 100px;
  }
}

.form-actions {
  display: flex;
  gap: 12px;
  justify-content: flex-end;
  margin-top: 24px;
}

// Feedback List
.feedback-list {
  h2 {
    font-size: 20px;
    margin: 0 0 16px 0;
  }
}

.feedback-card {
  background: rgba(255, 255, 255, 0.9);
  border-radius: 14px;
  padding: 20px;
  border: 1px solid rgba(15, 118, 110, 0.12);
  box-shadow: var(--shadow-glass);
  margin-bottom: 16px;
  
  .feedback-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
    
    .feedback-date {
      font-size: 13px;
      color: var(--text-secondary);
    }
  }
  
  .agreement-badge {
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 13px;
    font-weight: 500;
    
    &.badge-success { background: #d4edda; color: #155724; }
    &.badge-warning { background: #fff3cd; color: #856404; }
    &.badge-danger { background: #f8d7da; color: #721c24; }
  }
  
  .feedback-meta {
    display: flex;
    gap: 16px;
    margin-bottom: 12px;
    font-size: 13px;
    color: var(--text-secondary);
  }
  
  .feedback-rating {
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 12px;
    
    .stars {
      font-size: 18px;
      color: #ffc107;
    }
    
    .helpful-badge {
      padding: 2px 8px;
      background: #d4edda;
      color: #155724;
      border-radius: 4px;
      font-size: 12px;
    }
  }
  
  .feedback-comment,
  .feedback-outcome {
    background: #f3faf8;
    padding: 12px;
    border-radius: 10px;
    margin-bottom: 12px;
    
    strong {
      display: block;
      font-size: 13px;
      color: #666;
      margin-bottom: 4px;
    }
    
    p {
      margin: 0;
    }
  }
  
  .feedback-actions {
    display: flex;
    justify-content: flex-end;
    
    .calibration-used {
      font-size: 13px;
      color: #28a745;
    }
  }
}

// Loading & Empty States
.loading-state {
  text-align: center;
  padding: 60px;
  
  .spinner {
    width: 40px;
    height: 40px;
    border: 3px solid #e9ecef;
    border-top-color: #4361ee;
    border-radius: 50%;
    animation: spin 1s linear infinite;
    margin: 0 auto 16px;
  }
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

.empty-state {
  text-align: center;
  padding: 60px;
  background: white;
  border-radius: 12px;
  
  .empty-icon {
    font-size: 64px;
    margin-bottom: 16px;
  }
  
  h3 {
    margin: 0 0 8px 0;
  }
  
  p {
    color: #666;
    margin: 0 0 24px 0;
  }
}

.error-banner {
  background: #f8d7da;
  color: #721c24;
  padding: 12px 16px;
  border-radius: 8px;
  margin-bottom: 16px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  
  button {
    background: none;
    border: none;
    font-size: 20px;
    cursor: pointer;
    color: #721c24;
  }
}
